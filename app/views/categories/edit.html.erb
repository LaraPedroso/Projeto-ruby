<div>
    <div class="container">
        <div class="card-header">
            <h4 class="font-weight-bolder">Categoria</h4>
            <p class="mb-3">Editando a categoria <%= @category.name %> </p>
        </div>
        <%= render partial: 'form' %>
    </div>
</div>