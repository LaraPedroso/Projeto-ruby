<div>
    <div class="container">
        <div class="card-header">
            <h4 class="font-weight-bolder">Post</h4>
            <p class="mb-0">Editando Post --<%= @post.name %> </p>
        </div>
        <%= render partial: 'form' %>
    </div>
</div>